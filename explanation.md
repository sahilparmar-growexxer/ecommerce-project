merge:
merge is basially use for merging two branches and it can be add in-between of branch

rebase:
rebase is use to merge two branch but it cannot add in-between. there is one base branch and other branch can merge on the tip of one branch




stash:
when we are not sure to commit , stash can be use for further use

commit:
commit can be use when we sure about changes to remain.





if we are pushing our branch on remote so there may occurred conflict of merging. so for rebasing purpose force push is necessary to resolve conflict. and branch can be rebase with each other.